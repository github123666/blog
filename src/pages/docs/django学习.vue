<template>  <MarkdownToVue markdown="## 将二维表装python对象模型
python manage.py inspectdb      
转换的文件默认utf-16e编码要改成utf-8  
配置django数据库时key要大小,小写报错
## 对objects.filter使用的sql语句理解
参数就相当于sql语句查询条件
## get(pk=1)
这里pk指定的是primary key
## 关于方反向查询
当一个模型的的foreign key 指向另外一个数据库,当实例该数据库时可以通过,另一个数据库_set(也可以自行设置名称),访问关联的对象" /> </template>